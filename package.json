{
  "name": "qiskit-event-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "test:e2e": "npm run build && playwright test",
    "verify": "npm run check && npm run test:e2e",
    "astro": "astro"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "astro": "^7.2.4",
    "typescript": "^6.0.3"
  }
}
